import React, { useCallback } from 'react'
import { DataRow } from '../../types/Types'
import { Label } from 'semantic-ui-react'

type RangeViewProps = {
}

export default function RangeView({}: RangeViewProps) {
    return (
        <Label>RangeView</Label>
    )
}